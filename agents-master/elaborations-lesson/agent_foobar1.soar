sp {elaboration*foo-bar*create
    (state <s> ^superstate nil)
    -->
    (<s> ^foo bar)}

sp {elaboration*foo-bar*write
    (state <s> ^foo bar)
    -->
    (write |^foo bar exists!|)
    (interrupt)}
